# GitHub token with read:packages scope.
# Required to fetch current published versions of @olwiba/* and @genesis/* packages.
# The packages are private — request access from the repository owner.
PACKAGES_TOKEN=ghp_...
